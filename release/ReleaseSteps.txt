#show date time
date
#check if there is someone use it
tail -f /var/log/tesseracttraining/tesseracttraining.log

#go to release folder
cd ~/src/TesseractOcrTraining


git pull

sudo systemctl restart tesstrain

#sudo systemctl stop tesstrain


#if there is release script
#cd release/version
#sudo python3 version_upgrade.python3

#restart tesstrain 
sudo systemctl restart tesstrain

#some sample code 
#sudo crontab -l
#editing:    sudo crontab -e   
#~/src/tesstrainenv

0 2 * * * find /var/www/tesseracttraining -name "*.traineddata" -type f -mtime +2 -delete
2 2 * * * find /var/www/tesseracttraining -name "*.traineddata_20*" -type f -mtime +2 -delete
4 2 * * * find /var/www/tesseracttraining -name "*.checkpoint" -type f -mtime +2 -delete
10 2 * * * find /var/www/tesseracttraining -name "*.zip" -type f -mtime +2 -delete
